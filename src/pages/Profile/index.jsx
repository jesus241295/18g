import { Outlet, Link } from "react-router-dom";
import "./Profile.css";

export default function Profile() {
  return (
    <div>
      <h2>Dirigir a editar</h2>
      {/* <nav>
        <Link to="editar">Editar</Link>
        <Outlet />
      </nav> */}
    </div>
  );
}
